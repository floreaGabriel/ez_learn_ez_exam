# Fișiere care NU trebuie copiate în html (nginx.conf e folosit separat de Dockerfile)
Dockerfile
docker-compose.yml
.dockerignore
README.md
**/.DS_Store
.git
.gitignore
diagrams/
# backend contor + sqlrunner + conquistador (NU fac parte din site-ul static).
# IMPORTANT: conquistador/ conține server.js + questions/*.yaml cu RĂSPUNSURILE
# corecte — nu trebuie servite static (anti-cheat). Audio-ul jocului stă în /audio/.
counter/
sqlrunner/
conquistador/
sql/db/
deploy/
docker-compose.yaml
.github/
